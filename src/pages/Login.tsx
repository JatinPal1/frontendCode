import { SideImageBox, LoginSection } from '../features/authentication'

const Login = () => {
    return (
        <div className="w-screen h-screen sm:flex">
            <SideImageBox />
            <LoginSection />
        </div>
    )
}

export default Login