import { SideImageBox, OTP } from "../features/authentication"

const OTPpage = () => {
    return (
        <div className="h-[calc(100vh_-_4rem)] w-screen flex">
            <SideImageBox />
            <OTP />
        </div>
    )
}

export default OTPpage