// import { SignUpForm } from "../features/authentication";
import { SideImageBox, SignUpSection } from "../features/authentication";
const Signup = () => {

    return (
        <div className="w-screen h-screen sm:flex">
            <SideImageBox />
            <SignUpSection />
        </div>
    );
};

export default Signup;
